<!-- task-contract:v1 -->

## 背景

（说明信息，不进契约文件的规范化字段。约束性要求不要只写在这里。）

## 分支建议

> 非强制。缺失或与实际 branch/sparse 不同，不构成硬门禁。

- 分支：`domain/slug`
- sparse：`0-meta` `.agents`

## 目标

（非空。写入契约文件。）

## 要求 / 实现细节

### [R1] 标题

正文。每个 `[R*]` 必须稳定、唯一。代码块里的伪字段不算。

## 验收

1. [A1 → R1] 明确关联当前 Contract 内存在的 R ID，不得写「全局」。
适用条件：（可选。没有则始终适用，不能标 not-applicable。）

## 允许改动范围

- `0-meta/lib/new/`
